import {KonvaEventObject} from 'konva/types/Node'
import {KonvaNodeEvent} from 'konva/types/types'

import {CanvasState} from './types'

export default (evt: KonvaEventObject<KonvaNodeEvent.click>, state: CanvasState) => {
  
}
