import React from 'react'
import {Circle, Layer, Line, Stage, Text} from 'react-konva'

export default (props: object) => {
  /*const scale = 7.0;
  const { start, end, text, style } = props.appState;

  return (
    <Stage width={2430} height={1558} style={style} onClick={props.evtHandler}>
      <Layer>
        <Line
          points={[start.x, start.y, end.x, end.y]}
          visible={end.visible}
          stroke={"black"}
        />
        <Circle
          visible={start.visible}
          x={start.x}
          y={start.y}
          radius={8}
          fill={"green"}
          stroke={"black"}
        />
        <Circle
          visible={end.visible}
          x={end.x}
          y={end.y}
          radius={8}
          fill={"red"}
          stroke={"black"}
        />
        <Text
          visible={text.visible}
          x={text.x}
          y={text.y}
          fontSize={32}
          text={Travel(start, end, scale)}
          fill={"black"}
          stroke={"white"}
          strokeWidth={1}
        />
      </Layer>
    </Stage>
  );*/
};
